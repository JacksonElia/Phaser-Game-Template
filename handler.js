class Handler {

    // Values
    entityIDCounter = 0;
    // Entity Dictionaries
    entities = {};
    
    update() {
        for (let key in this.entities) {
            let entity = 
            entity.update();
        }
    }

}